import { ITestableProps } from 'lens-ui';
import React from 'react';

export interface <%=componentPropsName%> extends React.PropsWithChildren<{}>, ITestableProps {
  className?: string;
  styles?: React.CSSProperties;
}
<% if (useStore) { %>
export interface <%=componentStoreInterfaceName%> {
  count: number;
  increment: () => void;
  decrement: () => void;
  incBy: (amount: number) => void;
  decBy: (amount: number) => void;
}
<% } %>
