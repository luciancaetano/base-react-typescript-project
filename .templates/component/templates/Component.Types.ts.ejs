import React from 'react';

export interface <%=componentPropsName%> extends React.PropsWithChildren<{}> {
  className?: string;
  onClick?: () => void;
}
<% if (useStore) { %>
export interface <%=componentStoreInterfaceName%> {
  count: number;
  increment: () => void;
  decrement: () => void;
  incBy: (amount: number) => void;
  decBy: (amount: number) => void;
}
<% } %>
